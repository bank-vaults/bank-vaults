apiVersion: "vault.banzaicloud.com/v1alpha1"
kind: "Vault"
metadata:
  name: "vault"
spec:
  size: 1
  config:
    storage:
      file:
        path: "/vault/file"
    listener:
      tcp:
        address: "127.0.0.1:8200"
        tls_disable: true
