apiVersion: v1
kind: Service
metadata:
  name: {{ template "vault.fullname" . }}-{{ .Values.unsealer.metrics.name }}
  labels:
    app: {{ template "vault.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
  annotations:
    {{- range $key, $value := .Values.unsealer.metrics.annotations }}
      {{ $key }}: {{ $value | quote }}
    {{- end }}
spec:
  clusterIP: None
  ports:
  - port: {{ .Values.unsealer.metrics.port }}
    protocol: TCP
    name: {{ .Values.unsealer.metrics.name }}
  selector:
    app: {{ template "vault.name" . }}
    release: {{ .Release.Name }}
