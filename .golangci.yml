run:
    skip-dirs:
        - operator

linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: github.com/banzaicloud/bank-vaults

linters:
    enable-all: true
    disable:
        - maligned
        - funlen
        - dupl
        - wsl
        - lll

        # TODO: fixme
        - gochecknoglobals
        - gochecknoinits
        - unparam
        - goconst
        - gocognit
        - errcheck
        - unconvert
        - whitespace
        - misspell
        - nakedret
        - golint
        - scopelint
        - gocritic
        - stylecheck
        - gocyclo
