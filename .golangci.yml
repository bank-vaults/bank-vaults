run:
    skip-dirs:
        - operator

linters-settings:
    golint:
        min-confidence: 0.1
    goimports:
        local-prefixes: github.com/banzaicloud/bank-vaults

linters:
    enable-all: true
    disable:
        - maligned
        - funlen
        - dupl
        - wsl
        - lll

        # TODO: fixme
        - godox
        - gochecknoglobals
        - gochecknoinits
        - goconst
        - gocognit
        - errcheck
        - whitespace
        - nakedret
        - golint
        - scopelint
        - gocritic
        - stylecheck
        - gocyclo
